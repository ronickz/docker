```bash
cat /etc/passwd
```

```bash
sudo usermod -aG docker rnk
```

```bash
sudo systemctl start docker
```

```bash
systemctl is-active docker
```

**`EXTRA UTIL`**
```bash
sudo apt install lnav
```